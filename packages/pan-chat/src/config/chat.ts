export const chatConfig = {
  maxInputLength: 4000,
  enableStreaming: true,
  enableLocalMock: true,
  maxLocalMessages: 200,
  defaultModel: 'default',
  autoCreateSessionOnFirstMessage: true,
  storageVersion: '1',
} as const
